/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs');
const path = require('node:path');

const root = process.cwd();
const pagePath = path.join(root, 'src', 'app', 'page.tsx');
const shellPath = path.join(root, 'src', 'components', 'FinancePageShell.tsx');
const loginPath = path.join(root, 'src', 'app', 'login', 'page.tsx');
const sessionHelperPath = path.join(root, 'src', 'lib', 'localSession.ts');
const dashboardRoutePath = path.join(root, 'src', 'app', 'api', 'local', 'dashboard', 'route.ts');

for (const p of [pagePath, shellPath, loginPath]) {
  if (!fs.existsSync(p)) throw new Error(`Required file not found: ${p}`);
}

const stamp = new Date().toISOString().replace(/[-:]/g,'').replace(/\..+/, '').replace('T','-');
const backupDir = path.join('C:\\djallows-finance-backups', `BEFORE-LOCAL-DASHBOARD-${stamp}`);
fs.mkdirSync(backupDir, { recursive: true });
fs.copyFileSync(pagePath, path.join(backupDir, 'page.tsx'));
fs.copyFileSync(shellPath, path.join(backupDir, 'FinancePageShell.tsx'));
fs.copyFileSync(loginPath, path.join(backupDir, 'login-page.tsx'));

const normalize = (s) => s.replace(/^\uFEFF/, '').replace(/\r\n/g, '\n');
let page = normalize(fs.readFileSync(pagePath, 'utf8'));
let shell = normalize(fs.readFileSync(shellPath, 'utf8'));
let login = normalize(fs.readFileSync(loginPath, 'utf8'));

// ---------------- PAGE IMPORTS ----------------
page = page.replace(/^import \{ supabase \} from "@\/lib\/supabase";\n/m, '');
page = page.replace(
`import {\n  getDashboardFinancialData,\n  type DashboardFinancialData,\n  type LedgerTransaction,\n} from "@/lib/dashboard";`,
`import type {\n  DashboardFinancialData,\n  LedgerTransaction,\n} from "@/lib/dashboard";`
);

if (!page.includes('type LocalDashboardResponse =')) {
  const constantsMarker = '// ============================================================\n// CONSTANTS\n// ============================================================';
  const idx = page.indexOf(constantsMarker);
  if (idx < 0) throw new Error('Dashboard constants marker not found. No files were changed.');
  const responseType = `type LocalDashboardResponse = {\n  success: boolean;\n  error?: string;\n  business: BusinessProfile;\n  memberRole: MemberRole;\n  financialData: DashboardFinancialData;\n  payrollSummary: PayrollDashboardSummary | null;\n  invoices: InvoiceRow[];\n  customers: CustomerRow[];\n  tobaskiSeasons: TobaskiSeason[];\n  tobaskiExpenses: TobaskiExpense[];\n  sheepSales: SheepSaleRow[];\n  tobaskiStock: TobaskiStockRow[];\n  stockAvailable: boolean;\n  historicalCustomerSales: HistoricalCustomerSale[];\n};\n\n`;
  page = page.slice(0, idx) + responseType + page.slice(idx);
}

const dashboardStart = '  // ==========================================================\n  // LOAD DASHBOARD\n  // ==========================================================';
const dashboardEnd = '  // ==========================================================\n  // TRANSACTIONS AND PERIOD\n  // ==========================================================';
const ds = page.indexOf(dashboardStart);
const de = page.indexOf(dashboardEnd);
if (ds < 0 || de < 0 || de <= ds) throw new Error('Dashboard load section markers not found. No files were changed.');

const newDashboardBlock = `  // ==========================================================\n  // LOAD LOCAL DASHBOARD\n  // ==========================================================\n\n  useEffect(() => {\n    let active = true;\n\n    async function loadDashboard() {\n      try {\n        setLoading(true);\n        setError(\"\");\n\n        const response =\n          await fetch(\n            \"/api/local/dashboard\",\n            {\n              cache: \"no-store\",\n            }\n          );\n\n        if (response.status === 401) {\n          router.replace(\"/login\");\n          return;\n        }\n\n        const data =\n          (await response.json()) as LocalDashboardResponse;\n\n        if (\n          !response.ok ||\n          !data.success\n        ) {\n          throw new Error(\n            data.error ||\n              \"Unable to load local dashboard.\"\n          );\n        }\n\n        if (!active) {\n          return;\n        }\n\n        setBusiness(data.business);\n        setFinancialData(data.financialData);\n        setInvoices(data.invoices ?? []);\n        setCustomers(data.customers ?? []);\n        setTobaskiSeasons(data.tobaskiSeasons ?? []);\n        setTobaskiExpenses(data.tobaskiExpenses ?? []);\n        setSheepSales(data.sheepSales ?? []);\n        setTobaskiStock(data.tobaskiStock ?? []);\n        setStockAvailable(data.stockAvailable ?? true);\n        setHistoricalCustomerSales(\n          data.historicalCustomerSales ?? []\n        );\n        setMemberRole(data.memberRole);\n        setPayrollSummary(data.payrollSummary);\n        setLoading(false);\n      } catch (loadError) {\n        console.error(loadError);\n\n        if (active) {\n          setError(\n            loadError instanceof Error\n              ? loadError.message\n              : \"Unable to load dashboard.\"\n          );\n\n          setLoading(false);\n        }\n      }\n    }\n\n    loadDashboard();\n\n    return () => {\n      active = false;\n    };\n  }, [router]);\n\n`;
page = page.slice(0, ds) + newDashboardBlock + page.slice(de);

// ---------------- SHELL ----------------
shell = shell.replace(/^import \{ supabase \} from "@\/lib\/supabase";\n/m, '');
shell = shell.replace(/type UserProfile = \{[\s\S]*?\};\n\ntype BusinessAccess = \{[\s\S]*?\};\n\n/, '');

const shellUserStart = '  // ==========================================================\n  // CURRENT USER\n  // ==========================================================';
const shellUserEnd = '  // ==========================================================\n  // PERMISSIONS\n  // ==========================================================';
const ss = shell.indexOf(shellUserStart);
const se = shell.indexOf(shellUserEnd);
if (ss < 0 || se < 0 || se <= ss) throw new Error('Finance shell user section markers not found. No files were changed.');

const newShellUserBlock = `  // ==========================================================\n  // CURRENT LOCAL USER\n  // ==========================================================\n\n  useEffect(() => {\n    let active = true;\n\n    async function loadCurrentUser() {\n      try {\n        const response =\n          await fetch(\n            \"/api/local/auth/session\",\n            {\n              cache: \"no-store\",\n            }\n          );\n\n        if (!response.ok) {\n          router.replace(\"/login\");\n          return;\n        }\n\n        const data =\n          (await response.json()) as {\n            success?: boolean;\n            profile?: {\n              username?: string | null;\n              full_name?: string | null;\n              platform_role?: string | null;\n              is_active?: boolean;\n            };\n            business_access?: {\n              access_role?: string | null;\n              active?: boolean;\n            };\n          };\n\n        if (\n          !data.success ||\n          !data.profile ||\n          data.profile.is_active === false ||\n          data.business_access?.active === false\n        ) {\n          await fetch(\n            \"/api/local/auth/logout\",\n            { method: \"POST\" }\n          );\n\n          router.replace(\"/login\");\n          return;\n        }\n\n        const fullName =\n          data.profile.full_name?.trim() ||\n          data.profile.username?.trim() ||\n          \"User\";\n\n        let resolvedRole: MemberRole = \"staff\";\n\n        if (\n          data.profile.platform_role ===\n          \"super_admin\"\n        ) {\n          resolvedRole = \"owner\";\n        } else if (\n          data.business_access?.access_role ===\n          \"owner\"\n        ) {\n          resolvedRole = \"owner\";\n        } else if (\n          data.business_access?.access_role ===\n          \"admin\"\n        ) {\n          resolvedRole = \"admin\";\n        } else if (\n          data.business_access?.access_role ===\n          \"viewer\"\n        ) {\n          resolvedRole = \"viewer\";\n        }\n\n        if (!active) {\n          return;\n        }\n\n        setLoginFullName(fullName);\n        setLoginRole(\n          getRoleLabel(\n            resolvedRole,\n            data.profile.platform_role\n          )\n        );\n        setMemberRole(resolvedRole);\n        setAccessChecked(true);\n      } catch (loadError) {\n        console.error(\n          \"Unable to load current local user:\",\n          loadError\n        );\n\n        if (active) {\n          router.replace(\"/login\");\n        }\n      }\n    }\n\n    loadCurrentUser();\n\n    return () => {\n      active = false;\n    };\n  }, [router]);\n\n`;
shell = shell.slice(0, ss) + newShellUserBlock + shell.slice(se);

shell = shell.replace(
`  async function handleSignOut() {\n    await supabase.auth.signOut();\n\n    router.replace(\n      \"/login\"\n    );\n\n    router.refresh();\n  }`,
`  async function handleSignOut() {\n    await fetch(\n      \"/api/local/auth/logout\",\n      {\n        method: \"POST\",\n      }\n    );\n\n    router.replace(\n      \"/login\"\n    );\n\n    router.refresh();\n  }`
);

// ---------------- LOGIN REDIRECT ----------------
login = login.replace('"/local-ready"', '"/"');

// ---------------- VERIFY PATCHES ----------------
if (page.includes('supabase')) throw new Error('Dashboard still contains Supabase references. No files were changed.');
if (shell.includes('supabase')) throw new Error('FinancePageShell still contains Supabase references. No files were changed.');
if (!login.includes('"/api/local/auth/login"')) throw new Error('Login page is not using the local login endpoint. No files were changed.');
if (!login.includes('router.replace(\n        "/"')) {
  console.warn('Warning: login redirect formatting differs, but the local login endpoint is present.');
}

const localSession = `import \"server-only\";\n\nimport {\n  createHash,\n} from \"node:crypto\";\n\nimport type {\n  NextRequest,\n} from \"next/server\";\n\nimport {\n  getLocalDb,\n} from \"@/lib/localDb\";\n\nconst COOKIE_NAME =\n  \"djallows_session\";\n\ntype SessionRow = {\n  user_id: string;\n  username: string;\n  email: string | null;\n  full_name: string | null;\n  platform_role: string;\n  is_active: number;\n  must_change_password: number;\n  expires_at: string;\n};\n\ntype AccessRow = {\n  business_id: string;\n  access_role: string;\n  active: number;\n};\n\nfunction hashToken(\n  token: string\n) {\n  return createHash(\"sha256\")\n    .update(token)\n    .digest(\"hex\");\n}\n\nexport function getLocalSessionUser(\n  request: NextRequest\n) {\n  const rawToken =\n    request.cookies.get(\n      COOKIE_NAME\n    )?.value;\n\n  if (!rawToken) {\n    return null;\n  }\n\n  const db = getLocalDb();\n  const now = new Date().toISOString();\n\n  db.prepare(\n    \`DELETE FROM local_sessions WHERE expires_at <= ?\`\n  ).run(now);\n\n  const session =\n    db.prepare(\n      \`\n        SELECT\n          u.id AS user_id,\n          u.username,\n          u.email,\n          u.full_name,\n          u.platform_role,\n          u.is_active,\n          u.must_change_password,\n          s.expires_at\n        FROM local_sessions s\n        INNER JOIN local_users u\n          ON u.id = s.user_id\n        WHERE s.token = ?\n          AND s.expires_at > ?\n        LIMIT 1\n      \`\n    ).get(\n      hashToken(rawToken),\n      now\n    ) as SessionRow | undefined;\n\n  if (\n    !session ||\n    session.is_active !== 1\n  ) {\n    return null;\n  }\n\n  const access =\n    db.prepare(\n      \`\n        SELECT\n          business_id,\n          access_role,\n          active\n        FROM business_user_access\n        WHERE user_id = ?\n        LIMIT 1\n      \`\n    ).get(\n      session.user_id\n    ) as AccessRow | undefined;\n\n  if (\n    session.platform_role !==\n      \"super_admin\" &&\n    (\n      !access ||\n      access.active !== 1\n    )\n  ) {\n    return null;\n  }\n\n  return {\n    user: session,\n    access: access ?? null,\n  };\n}\n`;

const dashboardRoute = `import {\n  NextRequest,\n  NextResponse,\n} from \"next/server\";\n\nimport {\n  getLocalDb,\n} from \"@/lib/localDb\";\n\nimport {\n  getLocalSessionUser,\n} from \"@/lib/localSession\";\n\nexport const runtime = \"nodejs\";\nexport const dynamic = \"force-dynamic\";\n\ntype AccountRow = {\n  id: string;\n  name: string;\n  account_type: string;\n  opening_balance: number;\n};\n\ntype TransactionRow = {\n  id: string;\n  transaction_number: string;\n  transaction_date: string;\n  transaction_type: string;\n  description: string;\n  amount: number;\n  account_id: string | null;\n  destination_account_id: string | null;\n  payment_method: string | null;\n  category_id: string | null;\n  category_name: string | null;\n};\n\nfunction num(value: unknown) {\n  const result = Number(value ?? 0);\n  return Number.isFinite(result) ? result : 0;\n}\n\nexport async function GET(\n  request: NextRequest\n) {\n  try {\n    const session =\n      getLocalSessionUser(request);\n\n    if (!session) {\n      return NextResponse.json(\n        {\n          success: false,\n          error: \"Unauthorised.\",\n        },\n        { status: 401 }\n      );\n    }\n\n    const businessId =\n      session.access?.business_id;\n\n    if (!businessId) {\n      return NextResponse.json(\n        {\n          success: false,\n          error: \"No Djallows Farm business access was found.\",\n        },\n        { status: 403 }\n      );\n    }\n\n    const db = getLocalDb();\n\n    const business =\n      db.prepare(\n        \`\n          SELECT id, name, trading_name\n          FROM businesses\n          WHERE id = ?\n          LIMIT 1\n        \`\n      ).get(businessId) as\n        | {\n            id: string;\n            name: string;\n            trading_name: string | null;\n          }\n        | undefined;\n\n    if (!business) {\n      throw new Error(\n        \"Djallows Farm business profile was not found.\"\n      );\n    }\n\n    const accountRows =\n      db.prepare(\n        \`\n          SELECT\n            id, name, account_type, opening_balance\n          FROM financial_accounts\n          WHERE business_id = ?\n            AND active = 1\n          ORDER BY name\n        \`\n      ).all(businessId) as unknown as AccountRow[];\n\n    const transactionRows =\n      db.prepare(\n        \`\n          SELECT\n            t.id,\n            t.transaction_number,\n            t.transaction_date,\n            t.transaction_type,\n            t.description,\n            t.amount,\n            t.account_id,\n            t.destination_account_id,\n            t.payment_method,\n            t.category_id,\n            c.name AS category_name\n          FROM transactions t\n          LEFT JOIN categories c\n            ON c.id = t.category_id\n          WHERE t.business_id = ?\n          ORDER BY t.transaction_date DESC,\n                   t.created_at DESC\n        \`\n      ).all(businessId) as unknown as TransactionRow[];\n\n    const transactions =\n      transactionRows.map((row) => ({\n        ...row,\n        amount: num(row.amount),\n      }));\n\n    const balances =\n      new Map<string, number>();\n\n    for (const account of accountRows) {\n      balances.set(\n        account.id,\n        num(account.opening_balance)\n      );\n    }\n\n    for (const transaction of transactions) {\n      const amount = num(transaction.amount);\n\n      if (\n        transaction.transaction_type === \"income\" ||\n        transaction.transaction_type === \"capital_introduced\"\n      ) {\n        if (transaction.account_id) {\n          balances.set(\n            transaction.account_id,\n            (balances.get(transaction.account_id) ?? 0) + amount\n          );\n        }\n      }\n\n      if (\n        transaction.transaction_type === \"expense\" ||\n        transaction.transaction_type === \"payroll\" ||\n        transaction.transaction_type === \"owner_withdrawal\"\n      ) {\n        if (transaction.account_id) {\n          balances.set(\n            transaction.account_id,\n            (balances.get(transaction.account_id) ?? 0) - amount\n          );\n        }\n      }\n\n      if (\n        transaction.transaction_type === \"transfer\"\n      ) {\n        if (transaction.account_id) {\n          balances.set(\n            transaction.account_id,\n            (balances.get(transaction.account_id) ?? 0) - amount\n          );\n        }\n\n        if (transaction.destination_account_id) {\n          balances.set(\n            transaction.destination_account_id,\n            (balances.get(transaction.destination_account_id) ?? 0) + amount\n          );\n        }\n      }\n    }\n\n    const accounts =\n      accountRows.map((account) => ({\n        id: account.id,\n        name: account.name,\n        accountType: account.account_type,\n        balance: balances.get(account.id) ?? 0,\n      }));\n\n    const totalIncome =\n      transactions\n        .filter((row) => row.transaction_type === \"income\")\n        .reduce((sum, row) => sum + row.amount, 0);\n\n    const totalExpenses =\n      transactions\n        .filter(\n          (row) =>\n            row.transaction_type === \"expense\" ||\n            row.transaction_type === \"payroll\"\n        )\n        .reduce((sum, row) => sum + row.amount, 0);\n\n    const financialData = {\n      totalIncome,\n      totalExpenses,\n      netPosition: totalIncome - totalExpenses,\n      cashBalance: accounts\n        .filter((row) => row.accountType === \"cash\")\n        .reduce((sum, row) => sum + row.balance, 0),\n      waveBalance: accounts\n        .filter((row) => row.accountType === \"wave\")\n        .reduce((sum, row) => sum + row.balance, 0),\n      bankBalance: accounts\n        .filter((row) => row.accountType === \"bank\")\n        .reduce((sum, row) => sum + row.balance, 0),\n      totalAvailable: accounts\n        .reduce((sum, row) => sum + row.balance, 0),\n      accounts,\n      transactions,\n    };\n\n    const invoices =\n      (db.prepare(\n        \`\n          SELECT\n            id, customer_id, invoice_number, invoice_date,\n            total_amount, amount_paid, balance_due, status,\n            tobaski_season_id\n          FROM invoices\n          WHERE business_id = ?\n          ORDER BY invoice_date DESC, created_at DESC\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          ...row,\n          total_amount: num(row.total_amount),\n          amount_paid: num(row.amount_paid),\n          balance_due: num(row.balance_due),\n        }));\n\n    const customers =\n      (db.prepare(\n        \`\n          SELECT id, name, active\n          FROM contacts\n          WHERE business_id = ?\n            AND contact_type = 'customer'\n          ORDER BY name\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          id: String(row.id),\n          name: String(row.name),\n          active: Number(row.active) === 1,\n        }));\n\n    const tobaskiSeasons =\n      (db.prepare(\n        \`\n          SELECT id, season_name, season_year, active\n          FROM tobaski_seasons\n          WHERE business_id = ?\n          ORDER BY season_year DESC\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          id: String(row.id),\n          season_name: String(row.season_name),\n          season_year: num(row.season_year),\n          active: Number(row.active) === 1,\n        }));\n\n    const tobaskiExpenses =\n      (db.prepare(\n        \`\n          SELECT id, amount, tobaski_season_id, tobaski_quantity\n          FROM transactions\n          WHERE business_id = ?\n            AND transaction_type = 'expense'\n            AND tobaski_season_id IS NOT NULL\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          id: String(row.id),\n          amount: num(row.amount),\n          tobaski_season_id:\n            row.tobaski_season_id == null\n              ? null\n              : String(row.tobaski_season_id),\n          tobaski_quantity:\n            row.tobaski_quantity == null\n              ? null\n              : num(row.tobaski_quantity),\n        }));\n\n    const sheepSales =\n      (db.prepare(\n        \`\n          SELECT id, invoice_id, tobaski_season_id\n          FROM sheep_sale_details\n          WHERE business_id = ?\n            AND tobaski_season_id IS NOT NULL\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          id: String(row.id),\n          invoice_id:\n            row.invoice_id == null ? null : String(row.invoice_id),\n          tobaski_season_id:\n            row.tobaski_season_id == null\n              ? null\n              : String(row.tobaski_season_id),\n        }));\n\n    const tobaskiStock =\n      (db.prepare(\n        \`\n          SELECT\n            s.id,\n            s.tobaski_season_id,\n            CASE\n              WHEN EXISTS (\n                SELECT 1\n                FROM sheep_sale_details d\n                WHERE d.tobaski_stock_id = s.id\n              ) THEN 'Sold'\n              ELSE 'Remaining'\n            END AS stock_status\n          FROM tobaski_sheep_stock s\n          WHERE s.business_id = ?\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          id: String(row.id),\n          tobaski_season_id: String(row.tobaski_season_id),\n          stock_status: String(row.stock_status ?? \"Remaining\"),\n        }));\n\n    const historicalCustomerSales =\n      (db.prepare(\n        \`\n          SELECT id, contact_id, transaction_date, amount\n          FROM transactions\n          WHERE business_id = ?\n            AND transaction_type = 'income'\n            AND reference_type = 'historical_excel_import'\n            AND contact_id IS NOT NULL\n          ORDER BY transaction_date DESC\n        \`\n      ).all(businessId) as unknown as Array<Record<string, unknown>>)\n        .map((row) => ({\n          id: String(row.id),\n          contact_id:\n            row.contact_id == null ? null : String(row.contact_id),\n          transaction_date: String(row.transaction_date),\n          amount: num(row.amount),\n        }));\n\n    const month =\n      new Date().toISOString().slice(0, 7);\n\n    const activeEmployees =\n      db.prepare(\n        \`\n          SELECT COUNT(*) AS total\n          FROM employees\n          WHERE business_id = ?\n            AND active = 1\n        \`\n      ).get(businessId) as { total: number };\n\n    const paidPayroll =\n      db.prepare(\n        \`\n          SELECT\n            COALESCE(SUM(pe.net_pay), 0) AS total,\n            COUNT(*) AS payments\n          FROM payroll_entries pe\n          INNER JOIN payroll_runs pr\n            ON pr.id = pe.payroll_run_id\n          WHERE pr.business_id = ?\n            AND pe.payment_status = 'paid'\n            AND substr(COALESCE(pe.payment_date, ''), 1, 7) = ?\n        \`\n      ).get(\n        businessId,\n        month\n      ) as { total: number; payments: number };\n\n    const payrollSummary =\n      session.user.platform_role === \"super_admin\" ||\n      session.access?.access_role === \"owner\" ||\n      session.access?.access_role === \"admin\"\n        ? {\n            active_employees: num(activeEmployees.total),\n            paid_this_month: num(paidPayroll.total),\n            payments_this_month: num(paidPayroll.payments),\n          }\n        : null;\n\n    const memberRole =\n      session.user.platform_role === \"super_admin\"\n        ? \"super_admin\"\n        : session.access?.access_role ?? \"staff\";\n\n    return NextResponse.json({\n      success: true,\n      business,\n      memberRole,\n      financialData,\n      payrollSummary,\n      invoices,\n      customers,\n      tobaskiSeasons,\n      tobaskiExpenses,\n      sheepSales,\n      tobaskiStock,\n      stockAvailable: true,\n      historicalCustomerSales,\n    });\n  } catch (error) {\n    console.error(\"LOCAL DASHBOARD ERROR:\", error);\n\n    return NextResponse.json(\n      {\n        success: false,\n        error:\n          error instanceof Error\n            ? error.message\n            : \"Unable to load local dashboard.\",\n      },\n      { status: 500 }\n    );\n  }\n}\n`;

fs.mkdirSync(path.dirname(sessionHelperPath), { recursive: true });
fs.mkdirSync(path.dirname(dashboardRoutePath), { recursive: true });

// All verifications passed, now write.
fs.writeFileSync(sessionHelperPath, localSession, 'utf8');
fs.writeFileSync(dashboardRoutePath, dashboardRoute, 'utf8');
fs.writeFileSync(pagePath, page, 'utf8');
fs.writeFileSync(shellPath, shell, 'utf8');
fs.writeFileSync(loginPath, login, 'utf8');

console.log('');
console.log('LOCAL DASHBOARD CONVERSION APPLIED');
console.log('Backup:', backupDir);
console.log('Dashboard: local SQLite');
console.log('Navigation session: local SQLite');
console.log('Login redirect: Dashboard');
